// import { create } from 'zustand';

// export const useModalStore = create((set) => ({
//   modal: null,
//   open: (name, props) => set({ modal: { name, props } }),
//   close: () => set({ modal: null }),
// }));
